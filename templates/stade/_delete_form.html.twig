<form method="post" action="{{ path('app_stade_delete', {'refStade': stade.refStade}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stade.refStade) }}">
    <button class="btn">Delete</button>
</form>
